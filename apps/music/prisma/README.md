# Music Prisma

Music uses the **shared** schema at [`packages/db/prisma/schema.template.prisma`](../../../packages/db/prisma/schema.template.prisma) (same `DATABASE_URL` as the Steam API and watch service).

Generate/push via `pnpm db:generate` / `pnpm db:push` from the repo root.
