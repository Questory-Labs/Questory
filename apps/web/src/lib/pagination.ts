/** Default page sizes for list and grid pagination across the web app. */

/** Admin enrichment job queue (`/admin/enrichment`). */
export const ADMIN_ENRICHMENT_PAGE_SIZE = 15;

/** Admin cron run history (`/admin/cron`). */
export const ADMIN_CRON_PAGE_SIZE = 15;

/** Enterprise telemetry trace list. */
export const TELEMETRY_PAGE_SIZE = 15;

/** Music album/track listen history on detail pages. */
export const MUSIC_DETAIL_LISTENS_PAGE_SIZE = 15;

/** Music charts (tops) tables. */
export const MUSIC_CHARTS_PAGE_SIZE = 15;

/** Music recent listening feed. */
export const MUSIC_LISTENING_PAGE_SIZE = 15;

/** Watch/read recent history and read library lists. */
export const MEDIA_HISTORY_PAGE_SIZE = 15;

/** Game grid layouts (friends library, multiplayer). */
export const GAME_GRID_PAGE_SIZE = 15;

/** Friends list grid (`/friends`). */
export const FRIENDS_PAGE_SIZE = 15;

/** Cost ROI best/worst value rankings (`/cost`). */
export const COST_ROI_PAGE_SIZE = 20;

/** Wishlist table rows (`/wishlist`). */
export const WISHLIST_PAGE_SIZE = 15;

/** Family shareable games and license conflicts (`/family/library`). */
export const FAMILY_LIBRARY_PAGE_SIZE = 15;

/** qMonitor local play sessions (`/sessions`). */
export const PLAY_SESSIONS_PAGE_SIZE = 15;

/** Steam library grid (`/library`). */
export const LIBRARY_PAGE_SIZE = 48;
