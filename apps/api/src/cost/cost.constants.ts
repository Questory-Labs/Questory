/** Top unplayed paid games on the cost summary shelfware list. */
export const COST_SHELFWARE_LIMIT = 10;

/** Default page size for paginated cost/ROI rankings. */
export const COST_ROI_PAGE_SIZE = 20;

/** Max page size for cost/ROI rankings. */
export const COST_ROI_PAGE_SIZE_MAX = 100;
