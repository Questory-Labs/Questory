# API Prisma

The canonical schema lives at [`packages/db/prisma/schema.template.prisma`](../../../packages/db/prisma/schema.template.prisma).

Generate / push from the repo root:

```bash
pnpm db:generate
pnpm db:push
```

Do not add models here.
