/**
 * @questorylabs/db owns the shared Prisma schema (Steam + music + watch).
 * Generated clients live under each app's `src/generated/prisma`.
 * Use `pnpm db:generate` / `pnpm db:push` from the repo root.
 */
export {};
